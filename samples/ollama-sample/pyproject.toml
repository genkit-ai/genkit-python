[project]
name = "ollama-sample"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "genkit",
  "genkit-ollama",
  "pydantic>=2.0.0",
]

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build.targets.wheel]
packages = ["src"]
